
import React from 'react';

const ApiKeySetupOverlay: React.FC = () => {
    // This component is obsolete as the API key is now handled securely on the server-side.
    // It is kept to prevent build errors from any potential lingering imports, but it renders nothing.
    return null;
};

export default ApiKeySetupOverlay;